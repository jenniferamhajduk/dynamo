# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Toggle embedding cache:
#   cache ON:  MM_EMBEDDING_CACHE_GB = "4"  (or any non-zero value)
#   cache OFF: MM_EMBEDDING_CACHE_GB = "0"
#
apiVersion: nvidia.com/v1alpha1
kind: DynamoGraphDeployment
metadata:
  name: qwen3-vl-agg-gb200
spec:
  pvcs:
    - create: false
      name: model-cache
    - create: false
      name: compilation-cache
  services:
    Frontend:
      componentType: frontend
      envs:
        - name: HF_HOME
          value: /home/dynamo/.cache/huggingface
        - name: DYN_REQUEST_PLANE
          value: tcp
      extraPodSpec:
        tolerations:
          - key: "dedicated"
            value: "user-workload"
            effect: "NoSchedule"
            operator: "Equal"
          - key: "dedicated"
            value: "user-workload"
            effect: "NoExecute"
            operator: "Equal"
        mainContainer:
          image: nvcr.io/nvstaging/ai-dynamo/vllm-runtime:1.0.0rc8-arm64
          imagePullPolicy: IfNotPresent
          workingDir: /workspace
      replicas: 1
      resources:
        requests:
          cpu: "1"
        limits:
          cpu: "1"
      subComponentType: null

    VllmWorker:
      componentType: worker
      envFromSecret: hf-token-secret
      extraPodSpec:
        tolerations:
          - key: "dedicated"
            value: "user-workload"
            effect: "NoSchedule"
            operator: "Equal"
          - key: "dedicated"
            value: "user-workload"
            effect: "NoExecute"
            operator: "Equal"
          - key: "nvidia.com/gpu"
            operator: "Exists"
            effect: "NoSchedule"
        mainContainer:
          command:
            - /bin/bash
            - -lc
          args:
            - |
              set -euo pipefail
              SITE_PACKAGES="$(python3 -c 'import pathlib, vllm; print(pathlib.Path(vllm.__file__).resolve().parent.parent)')"
              cd "${SITE_PACKAGES}"
              curl -sL https://github.com/vllm-project/vllm/pull/34182.diff | patch -p1
              curl -sL https://github.com/vllm-project/vllm/pull/34783.diff | python3 -c "
              import sys
              chunks = sys.stdin.read().split('diff --git ')
              filtered = [c for c in chunks if c.startswith('a/vllm/')]
              print(''.join('diff --git ' + c for c in filtered))
              " | patch -p1
              cd /workspace

              python3 -m dynamo.vllm \
                --model Qwen/Qwen3-VL-30B-A3B-Instruct-FP8 \
                --enable-multimodal \
                --tensor-parallel-size 1 \
                --gpu-memory-utilization 0.85 \
                --max-model-len 16384 \
                --disable-log-requests \
                --enable-prefix-caching \
                --multimodal-embedding-cache-capacity-gb "${MM_EMBEDDING_CACHE_GB}"
          image: nvcr.io/nvstaging/ai-dynamo/vllm-runtime:1.0.0rc8-arm64
          imagePullPolicy: IfNotPresent
          env:
            - name: HF_HOME
              value: /home/dynamo/.cache/huggingface
            - name: DYN_REQUEST_PLANE
              value: tcp
            - name: DYN_VLLM_EMBEDDING_TRANSFER_MODE
              value: nixl-write
            - name: MM_EMBEDDING_CACHE_GB
              value: "4"  # MM_EMBEDDING_CACHE_GB
          workingDir: /workspace
      replicas: 1
      resources:
        limits:
          gpu: "1"
        requests:
          gpu: "1"
      subComponentType: null
      volumeMounts:
        - name: model-cache
          mountPoint: /home/dynamo/.cache/huggingface
        - name: compilation-cache
          mountPoint: /home/dynamo/.cache/vllm
          useAsCompilationCache: true
